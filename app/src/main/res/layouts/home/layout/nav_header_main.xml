<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingTop="@dimen/header_p"
    android:paddingStart="@dimen/header_p"
    android:paddingEnd="@dimen/header_p"
    android:paddingBottom="@dimen/header_p_b"
    android:layout_marginTop="@dimen/header_p"
    android:background="@color/colorPrimary">

    <ImageView
        android:layout_width="@dimen/header_avatar_size"
        android:layout_height="@dimen/header_avatar_size"
        android:contentDescription="@string/content_description"
        app:srcCompat="@drawable/ic_avatar" />

    <TextView
        style="@style/MediumField"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/header_title_text"
        android:text="Owner" />

    <TextView
        style="@style/LightField"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/header_subtitle_text"
        android:text="POS 1"/>

</LinearLayout>
